import theme, { palette, colors, dWidth, dHeight, dimensions } from './theme';
import { ApiConstant } from './apiConstant';

export { theme, palette, colors, dWidth, dHeight, dimensions, ApiConstant };
